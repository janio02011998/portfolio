import React from 'react';

import { Carousel, Image } from 'react-bootstrap';
import { Container, Title } from './styles';

function Main() {
    return (
        <Container>
            <Title>Início</Title>
        </Container>
    );
}

export default Main;
